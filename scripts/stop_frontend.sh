#!/bin/bash
pkill -f bike-index-platform/frontend

# kills processes that match "bike-index-platform/frontend"
#
# pkill --- send signal to process based on their name or attritubes
# -f --- option for matching anything after this flag
